# Alephant Agent Version Notice

OpenCore reads `stable.json` to display Alephant Agent version updates in the
administrator Runtime Version Updates page.

This repository is informational only. OpenCore does not update containers from
this file. When a newer version is displayed, administrators contact the
Alephant team to coordinate the server-side update.
